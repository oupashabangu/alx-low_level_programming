#include "main.h"

/**
 * main - Print the alphabet in lowercase followed by new line
 *
 * Return: void
 */
void print_alphabet(void)
{
	print_alphabet(c);

	for (c = 'a'; c <= 'z'; c++)
	{
		_putchar(c);
	}

	putchar('\n');
}
