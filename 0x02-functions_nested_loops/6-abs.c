#include "main.h"

/**
 * _abs - compute absolute value of interger
 *
 * @n: interger arguments
 *
 * Return: int or int times negative
 */
int _abs(int n)
{
	if (n > 0)
		return (n);
	else
		return (n * -1);
}
